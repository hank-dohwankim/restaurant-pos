<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.system.restaurant.repository.MenuRepository">

    <select id="menulist" resultMap="com.system.restaurant.domain.Menu">
        select menu_id, menu_name, menu_price, cooking_time from tbl_menu
    </select>

    <select id="findOne" parameterMap="int" resultMap="com.system.restaurant.domain.Menu">
        select menu_id, menu_name, menu_price, cooking_time from tbl_menu
        where menu_id = #{id}
    </select>

    <insert id="post" parameterMap="com.system.restaurant.domain.Menu">
        insert into tbl_menu(menu_name, menu_price, cooking_time)
        valuse (#{menu_name}, #{menu_price}, #{cooking_time})
    </insert>

    <update id="put" parameterMap="com.system.restaurant.domain.Menu">
        update tbl_menu set
        menu_name = #{menu_name},
        menu_price = #{menu_price},
        cooking_time = #{cooking_time}
        where menu_id =#{id}
    </update>

     <delete id="delete" parameterMap="int">
         delete tbl_menu where menu_id = #{id}
     </delete>

</mapper>



